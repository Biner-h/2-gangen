function multiplication() {
    let stopValue = 20;
    for (counter = 2; counter <= stopValue; counter += 2) {
        document.getElementById("nummer").innerHTML += `<p>` + counter + `</p>`;
    }
}
